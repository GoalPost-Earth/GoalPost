Function StaticPictureWrapper
A React component that creates a static picture of a graph using NVL. This component is useful for generating static visualizations of graphs without requiring user interaction. NVL is destroyed after capturing the image to free up resources.

StaticPictureWrapper(__namedParameters): null | Element
Parameters
__namedParameters: StaticPictureWrapperProps
Returns null | Element
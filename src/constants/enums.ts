export enum CloudinaryPresets {
  MemberPhotos = 'member-photos',
}

export enum FormMode {
  Create = 'Create',
  Update = 'Update',
}

export enum EntityEnum {
  Community = 'Community',
  Person = 'Person',
  Resource = 'Resource',
  Goal = 'Goal',
  CoreValue = 'CoreValue',
  CarePoint = 'CarePoint',
}
